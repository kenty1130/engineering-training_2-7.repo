<h1>
  グループ
</h1>
<nav>
  <%= link_to 'ホームへ', footsteps_path %>
  <%= link_to 'グループ作成', new_group_path %>
</nav>
<div class="groups">
  <h2>グループ一覧</h2>
  <ul>
    <% @groups.each do |group| %>
      <li>
        <%= link_to group.group_name, group_path(group) %>
      </li>
    <% end %>
  </ul>
</div>